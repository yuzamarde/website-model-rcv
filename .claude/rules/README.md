# Rules

| Rule | Scope |
|---|---|
| [additive-only.md](additive-only.md) | Response-shape evolution — add only, never remove/rename/retype; protects deployed buyer bundles + porto-rs parity. Edit-origin = porto-be. Pre-publish gate before tagging. |

This repo has exactly one concern: keeping the public response contract stable + additive.
The authoritative overview is the repo `CLAUDE.md`.
